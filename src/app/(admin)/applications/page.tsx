import { ApplicationListContent } from "@/components/partials/ApplicationList";

export const metadata = { title: "คำขอทั้งหมด" };

export default function ApplicationsPage() {
  return <ApplicationListContent />;
}
