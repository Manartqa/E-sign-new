import { SignerFormContent } from "@/components/partials/Signer";

export const metadata = { title: "เพิ่มผู้มีอำนาจลงนาม" };

export default function NewSignerPage() {
  return <SignerFormContent />;
}
