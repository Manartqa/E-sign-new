import { SigningWorkflowListContent } from "@/components/partials/SigningWorkflow";

export const metadata = { title: "กระบวนการลงนาม" };

export default function SigningWorkflowsPage() {
  return <SigningWorkflowListContent />;
}
